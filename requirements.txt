# Pipeline données
pandas
numpy
requests

# Modèle — un seul moteur d'inférence (NumPyro/JAX) pour tout le dépôt :
# calibration du saut terminal (model/core/terminal_jump.py) comme modèles live,
# dans la même syntaxe.
# xarray : format des paramètres calibrés (model/core/bank.py) et des tirages de
# nowcast (model/core/base.py:Nowcast) — accès par coordonnées nommées
# (institut, bloc, candidat...) plutôt que par index entier.
numpyro
jax
xarray
# scipy : Cholesky du postérieur GP (`cho_factor`/`cho_solve`) et optimisation
# REML. Dépendance DIRECTE, pas transitive — elle doit être déclarée même si
# jax/numpyro l'installent au passage.
scipy

# Ingestion sondages (sondages/wiki.py, source primaire) + parsing PDF (spot-check)
mwparserfromhell
pdfplumber
# Fallback d'extraction par LLM (sondages/extract_llm.py) — spot-check PDF
# ponctuel uniquement, plus utilisé en ingestion primaire. Optionnel, activé
# par SONDAGES_USE_LLM (cf. sondages/requirements-llm.txt).
anthropic>=0.40

# Tests
pytest

# Retirés de cette branche (aucun import DEPUIS LE REGISTRE) :
#   dynamax    — filtre de Kalman du nowcast SSM. Le code de
#     `model/models/bayesian_nowcast/` est présent dans l'arbre, mais
#     `model/core/registered.py` ne l'importe pas ici : ce modèle ne tourne que
#     sur `dev`. Les tests qui le touchent skippent d'eux-mêmes
#     (`pytest.importorskip("dynamax")`).
#   matplotlib — servait aux notebooks d'exploration, non versionnés ;
#   pyarrow    — plus aucun format colonne en lecture/écriture ;
#   scipy==1.12.0 — épinglé pour arviz 0.17 (`scipy.signal.gaussian`, retiré en
#     1.13), or arviz n'est pas une dépendance ici. scipy reste installé de façon
#     transitive par jax/numpyro, sans contrainte de version — ce qui lève au
#     passage la restriction Python 3.9 documentée jusqu'ici.
