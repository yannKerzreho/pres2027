# Pipeline données & exploration
pandas
numpy
matplotlib<3.9.0
requests
pyarrow

# Modèle bayésien — un seul moteur (NumPyro/JAX) pour la calibration hiérarchique
# ET les modèles live : tout modèle du dépôt (calibration incluse) s'écrit dans
# la même syntaxe NumPyro, cf. model/models/bayesian_nowcast.py.
# xarray : format du résultat de calibration (model/core/bank.py) et des
# tirages de nowcast (model/core/base.py:Nowcast) — accès par coordonnées
# nommées (institut, bloc, candidat...) plutôt que par index entier.
numpyro
jax
xarray
scipy==1.12.0

# Filtre de Kalman JAX pur (dynamax.linear_gaussian_ssm) pour marginaliser
# analytiquement le chemin gaussien du nowcast SSM (cf. docs/spec_ssm_nowcast.md
# §4) — NumPyro garde le rôle d'orchestrateur des hyperparamètres, dynamax
# calcule la vraisemblance marginale des sondages injectée via numpyro.factor.
dynamax==0.1.8

# Ingestion sondages (sondages/wiki.py, source primaire) + parsing PDF (spot-check)
mwparserfromhell
pdfplumber
# Fallback d'extraction par LLM (sondages/extract_llm.py) — spot-check PDF
# ponctuel uniquement, plus utilisé en ingestion primaire. Optionnel, activé
# par SONDAGES_USE_LLM (cf. sondages/requirements-llm.txt).
anthropic>=0.40

# Tests
pytest
