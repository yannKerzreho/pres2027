[pytest]
# Collecte restreinte aux deux suites du dépôt. Sans ça, pytest ramasse aussi
# des scripts exploratoires dont le nom finit par `_test.py` (notebooks/),
# qui ne sont pas des tests et cassent la collecte — donc toute la CI.
testpaths = tests sondages/tests
addopts = -q
